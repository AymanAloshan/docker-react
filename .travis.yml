sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t Ayman/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Ayman/docker-react npm run test -- --coverage
