sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t AymanAloshan/docker-react -f Dockerfile.dev .

script:
  - docker run AymanAloshan/docker-react npm run test -- --coverage
